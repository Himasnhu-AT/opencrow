{
  "name": "sample-website",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "main": "demo.js",
  "scripts": {
    "dev": "concurrently \"python3 -m http.server 8000\" \"node mock-server.js\"",
    "format": "prettier --write \"src/**/*.ts\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.22.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}