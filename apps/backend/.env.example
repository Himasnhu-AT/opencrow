# NODE_ENV=production
GEMINI_API_KEY="<API_KEY>"
DATABASE_URL="postgresql://postgres:password@localhost:5432/opencrow"

ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=password123
JWT_SECRET=supersecretkey

# LLM Provider Configuration
# Options: "gemini", "ollama", or "openai"
LLM_PROVIDER=gemini

# Ollama Configuration (only needed if LLM_PROVIDER=ollama)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# OpenAI-compatible Configuration (only needed if LLM_PROVIDER=openai)
# Works with Modal.com, Together AI, or any OpenAI-compatible endpoint
OPENAI_BASE_URL=https://your-modal-deployment.modal.run/v1
OPENAI_API_KEY=your-api-key
OPENAI_MODEL=your-model-name
